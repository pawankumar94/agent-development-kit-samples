# Google AI Configuration (Required)
GOOGLE_API_KEY=your_google_ai_api_key_here

# Model Configuration (Optional)
DEFAULT_MODEL=gemini-2.0-flash
FALLBACK_MODEL=gemini-1.5-flash

# Application Configuration (Optional)
DEFAULT_APP_NAME=adk_samples_comprehensive
DEFAULT_USER_ID=sample_user
DEFAULT_SESSION_ID=sample_session

# Retry and Timeout Configuration (Optional)
MAX_RETRIES=3
RETRY_DELAY=1.0
BACKOFF_MULTIPLIER=2.0
DEFAULT_TIMEOUT=30
LONG_TIMEOUT=120

# Logging Configuration (Optional)
LOG_LEVEL=INFO
VERBOSE_LOGGING=false
DEBUG_MODE=false

# Development and Testing (Optional)
TEST_MODE=false
MOCK_EXTERNAL_APIS=false

# Sample-Specific Defaults (Optional)
DEFAULT_WEATHER_CITY=San Francisco
DEFAULT_NEWS_CATEGORY=technology
DEFAULT_STOCK_SYMBOL=GOOGL 
